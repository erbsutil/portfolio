---
title: "Diário Fit — Backend & Bot Telegram"
role: "Fullstack Engineer & Product Owner"
year: 2025
duration: "Ongoing"
teamSize: 1
outcomeSummary: "Bot de diário alimentar e de treino em produção no Telegram, com análise multimodal via Gemini, persistência em Supabase e zero infraestrutura gerenciada — rodando inteiramente sobre Windmill."
overview: "O Diário Fit é um sistema de rastreamento de alimentação e treinos construído para uso familiar. O backend vive inteiramente no Windmill — uma plataforma de workflows serverless com runtime Bun — e expõe o produto via bot no Telegram. Meu papel foi de engenheiro e dono do produto: projetei a arquitetura, escrevi todos os scripts, defini o protocolo de IA e mantive o sistema em evolução contínua (versão 11.0.0 em abril/2026)."
problem: "A família precisava de uma forma de registrar refeições e treinos no dia a dia sem fricção — sem formulários, sem apps extras para instalar. O canal já existente era o Telegram. O desafio técnico era transformar mensagens livres em texto, foto e voz em dados estruturados e persistidos de forma confiável, com contexto de histórico, fusos horários corretos e múltiplos utilizadores num mesmo grupo de chat."
constraints:
  - "Zero infraestrutura própria: sem servidor, sem container, sem cron job manual — tudo via Windmill."
  - "Runtime Bun no worker do Windmill não suporta imports relativos entre scripts, obrigando bundle único por esbuild."
  - "API do Telegram encerra conexão webhook após ~60 s; jobs com Gemini + ElevenLabs se aproximam desse limite."
  - "Custo zero de GPU: análise de imagem, transcrição de voz e geração de texto devem reutilizar a mesma chave Gemini."
  - "Grupo de Telegram: bot precisa ignorar ruído de conversa sem ser invasivo, mas capturar mensagens de dieta/treino sem falsos negativos."
  - "Idempotência obrigatória: webhook pode entregar o mesmo update mais de uma vez em retentativas."
approach: "Adotei o Windmill como plataforma de execução para eliminar qualquer gerenciamento de servidor. O ponto de entrada é um webhook do Telegram que dispara um job Bun. Toda a lógica do bot vive em módulos TypeScript em src/, compilados num bundle único via esbuild antes de cada deploy (wmill sync push). O Gemini 2.5 Flash centraliza análise de texto, interpretação de foto e transcrição de voz — uma única chave de API para três responsabilidades distintas. O Supabase (PostgreSQL) armazena perfis, entradas diárias e treinos, com RLS desabilitado intencionalmente pois o acesso vem exclusivamente do service_role do Windmill. A lógica de 'dia civil' (timezone-aware) foi extraída em localDay.ts, espelhado entre o backend e o frontend para garantir consistência sem criar um pacote compartilhado formal."
keyDecisions:
  - decision: "Windmill como plataforma de execução em vez de servidor Node/Bun próprio"
    reasoning: "Elimina gerenciamento de infra, restart de processos, deploy manual e cron jobs separados. O Windmill entrega logs estruturados por execução, retry automático e agendamento nativo — tudo que um servidor Express simples exigiria operacionalmente."
    alternatives:
      - "Railway ou Fly.io com servidor Express/Hono"
      - "Cloudflare Workers (descartado pelo limite de runtime e ausência de Bun nativo)"
  - decision: "Bundle único por esbuild em vez de múltiplos scripts com imports relativos"
    reasoning: "O loader Bun no worker do Windmill falha ao resolver imports relativos entre arquivos. Bundlar tudo num único arquivo main.ts entregável resolve o problema sem alterar a estrutura de desenvolvimento, que mantém módulos separados em src/."
    alternatives:
      - "Inlining manual do código (descartado: inviável de manter)"
      - "Usar Deno runtime (descartado: exigiria reescrever dependências e afastaria do ecossistema npm)"
  - decision: "Gemini como modelo único para texto, visão e transcrição de voz"
    reasoning: "Reduz o número de chaves de API, simplifica o rate limiting e mantém o custo operacional próximo de zero para o volume familiar. A transcrição via Gemini tem qualidade equivalente ao Whisper para PT-BR no contexto de alimentação e treino."
    alternatives:
      - "OpenAI Whisper para transcrição (mantido como opcional via OPENAI_API_KEY, usado se disponível)"
      - "Modelo local de visão (inviável sem GPU própria)"
  - decision: "Gate de grupo com heurísticas de score em vez de comandos explícitos"
    reasoning: "Num supergrupo familiar, exigir / ou menção para cada mensagem criaria fricção inaceitável. O gate pondera palavras de dieta, verbos de treino, modalidades (zumba, natação, maratona…), duração por extenso e padrões de vocativo (para não responder a perguntas filho→mãe). Isso permite que o bot 'ouça' organicamente sem invadir conversas irrelevantes."
    alternatives:
      - "Modo só-comando (descartado: os utilizadores não querem digitar prefixos para cada refeição)"
      - "IA para decidir relevância (descartado: custo e latência para mensagens que devem ser ignoradas silenciosamente)"
  - decision: "localDay.ts espelhado manualmente entre backend e frontend"
    reasoning: "A lógica de janela UTC do dia civil precisa ser idêntica nos dois projetos para que as queries do bot e do dashboard retornem os mesmos registos. Criar um pacote npm compartilhado adicionaria complexidade de publicação e versionamento desnecessária para um time de uma pessoa. O espelho manual com teste automatizado (test_local_day.mjs) garante sincronização sem overhead."
    alternatives:
      - "Pacote npm privado compartilhado (descartado: overhead de publish/versioning)"
      - "Duplicação sem teste (descartado: divergência silenciosa entre projetos)"
  - decision: "Idempotência via update_id gravado no banco antes de chamar a IA"
    reasoning: "O Telegram faz retentativas se o webhook não responder a tempo. Sem idempotência, a mesma refeição poderia ser gravada duas vezes. Checar o update_id antes de qualquer chamada ao Gemini é a barreira mínima que resolve o problema com uma query simples."
    alternatives:
      - "Redis/cache externo para deduplicação (descartado: dependência extra sem ganho real no volume atual)"
techStack:
  - "TypeScript (Bun runtime)"
  - "Windmill (plataforma serverless de workflows)"
  - "Supabase (PostgreSQL + REST API)"
  - "Google Gemini 2.5 Flash (análise de texto, visão e transcrição de voz)"
  - "ElevenLabs (TTS restrito a momentos específicos)"
  - "Telegram Bot API (webhook)"
  - "esbuild (bundler para deploy)"
  - "Vitest (testes unitários)"
  - "OpenAI Whisper (opcional, transcrição de voz)"
  - "Sentry (monitoramento de erros, opcional)"
impact:
  metrics:
    - label: "Infraestrutura gerenciada"
      value: "0 servidores"
    - label: "APIs de IA distintas"
      value: "1 (Gemini centralizado)"
    - label: "Versão atual"
      value: "11.0.0"
    - label: "Modalidades de entrada suportadas"
      value: "Texto, foto, voz, foto+texto em duas mensagens"
  qualitative: "O bot está em uso diário por uma família real, registrando refeições e treinos sem formulários ou apps adicionais. O sistema sobreviveu a 11 versões de evolução sem downtime planejado, incorporando edição granular de registros, resumo semanal agendado, suporte a grupos e fluxo de autorização multi-usuário — tudo sem sair da plataforma Windmill."
learnings:
  - "Bundling como solução de compatibilidade: quando o ambiente de execução impõe restrições ao sistema de módulos, uma etapa de build resolve o problema sem comprometer a organização do código-fonte."
  - "Gate de relevância em grupo é um produto, não só código: as heurísticas de filtragem de mensagens exigiram mais iterações do que qualquer outra parte técnica, porque o critério de 'relevante' é essencialmente uma decisão de UX."
  - "Espelhar lógica crítica com teste automatizado é melhor do que criar abstração prematura: o localDay.ts duplicado com suite de testes provou ser mais seguro do que um pacote compartilhado sem processo de publicação definido."
  - "Idempotência barata vale mais do que idempotência sofisticada: checar um campo no banco antes de chamar a IA resolve 99% dos casos de retentativa do Telegram sem adicionar dependências."
  - "Modelo único de IA para múltiplas modalidades reduz superfície de falha: menos chaves, menos rate limits distintos, menos casos de erro para tratar."
featured: true
status: "ongoing"
order: 2
isMeta: false
relatedProjects: []
relatedDecisions: []
---
